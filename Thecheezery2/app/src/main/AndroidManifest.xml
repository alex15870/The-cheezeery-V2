<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="molina.arenasl.thecheezery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".btn6_pantalla"></activity>
        <activity android:name=".btn5_pantalla" />
        <activity android:name=".btn4_pantalla" />
        <activity android:name=".btn3_pantalla" />
        <activity android:name=".btn2_pantalla" />
        <activity android:name=".btn1_pantalla" />
        <activity android:name=".Pantalla3" />
        <activity android:name=".Registro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>